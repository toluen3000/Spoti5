package com.example.spoti5.domain.use_cases

import com.example.spoti5.domain.model.UserModel
import com.example.spoti5.domain.repository.SpotifyRepository
import retrofit2.Response
import javax.inject.Inject

//class UserInforUseCase @Inject constructor(private val repository: SpotifyRepository){
//
//        suspend operator fun invoke(): Response<UserModel> = repository.getCurrentUser()
//}